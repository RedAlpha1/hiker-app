package com.ridgeline.ui

/**
 * Not implemented yet. Per CLAUDE.md this module owns the Compose
 * Multiplatform screens (library, summary, detail, onboarding) plus the
 * AR viewfinder overlay drawn over the native camera preview. Screen state
 * as plain data classes — see CLAUDE.md, "Conventions".
 */
